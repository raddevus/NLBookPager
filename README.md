### Buttons currently connected

GPIO 4 -> GND = NEXT_PIN (moves Kindle book to next page) <br>
GPIO 5 -> GND = PREV_PIN (moves Kindle book to prev page) <br>
GPIO 15 -> GND = LOCK_SCREEN_PIN (locks ipad / turns off screen) <br>
